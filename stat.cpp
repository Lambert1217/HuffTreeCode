#include "stat.h"

void stat(mychar* chars,int num,QString filename){
    /*
      filename是文本文件的地址
      chars是字符数组的结构体数组  结构体在config里面定义了
      num是频度不为0的字符的个数

      函数要逐个统计文本文件的字符，然后把频度为零的删掉，num要记好。
    */
}
