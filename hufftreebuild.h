#ifndef HUFFTREEBUILD_H
#define HUFFTREEBUILD_H

#include "config.h"
#include "huftree.h"

//构建哈夫曼树
template <class T>
HufTree<T>* huffTreeBuild(mychar* chars,int num){
    //chars是字符数组的结构体数组  结构体在config里面定义了
    //num是频度不为0的字符的个数
}

#endif // HUFFTREEBUILD_H
